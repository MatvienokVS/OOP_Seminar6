package work2;

// Генератор отчета в формате JSON
public class JsonReportGenerator implements ReportGenerator {
    @Override
    public String generateReport() {
        String report = "";
        // Логика генерации отчета в формате JSON
        return report;
    }
}
